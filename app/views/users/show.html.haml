%p#notice= notice

#players
  = render :partial => 'players'

= link_to 'Edit', edit_user_path(@user)
\|
= link_to 'Back', users_path

= javascript_tag do
  $(document).ready( |
    function(){ |
      setInterval(function(){ |
        $('#players').load("/refresh?date=#{Date.today.to_s}&user_id=#{@user.id}"); |
      }, 30000); |
    }); |
