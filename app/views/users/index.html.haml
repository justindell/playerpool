%h1 Listing users

%table
  %tr
    %th First name
    %th Last name
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.first_name
      %td= user.last_name
      %td= link_to 'Show', user
      %td= link_to 'Edit', edit_user_path(user)
